{
  "name": "agave-file-browser-demo",
  "private": true,
  "version": "0.1.0",
  "dependencies": {
    "angular": "~1.5.0"
  },
  "devDependencies": {
    "http-server": "^0.9.0"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1 ./src"
  }
}